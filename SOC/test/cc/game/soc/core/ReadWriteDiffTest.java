package cc.game.soc.core;

import java.io.*;

import junit.framework.TestCase;

public class ReadWriteDiffTest extends TestCase {

    public void testWrite() throws Exception {
        /*
        SOC soc = new SOC();
        BufferedReader in = new BufferedReader(new FileReader("testresources/games/game1.txt"));
        soc.read(in);
        in.close();
        
        BufferedWriter writer = new BufferedWriter(new PrintWriter(System.out));
        soc.write2(writer);
        writer.close();
        */
    }
    
}

