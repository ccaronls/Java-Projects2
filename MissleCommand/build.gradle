apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'cc.misslecommand.swing.MCApplet'

dependencies {
    implementation project(':GameLibrary')
}

sourceSets {
    main {
        java {
            srcDirs = ['src']
            //, '../GameLibrary/src']
            //exclude '**/swing/**'
            //exclude '**/skunkworks/**'
        }

        resources {
            srcDirs = ['resources'];
        }
    }
}

