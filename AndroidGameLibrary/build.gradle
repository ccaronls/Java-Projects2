apply plugin: 'com.android.library'

dependencies {
    //    compile fileTree(dir: '../GameLibrary/src');
    //    compile fileTree(dir: 'libs', include: '*.jar')
    //    compile fileTree(dir: '../GameLibrary/src', exclude:['**/skunkworks/**', '**/apps/**', '**/net/**', '**/swing/**']);
    //    compile project(':GameLibrary')
    //    compile project(
}

android {
    compileSdkVersion project.myCompileSdkVersion.toInteger()
    buildToolsVersion project.myBuildToolsVersion

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }

    defaultConfig {
	    minSdkVersion project.myMinSdkVersion
        targetSdkVersion project.myTargetSdkVersion
        versionCode 1
	    versionName '1.0.0'

    	sourceSets {
        	main {
           		manifest.srcFile 'AndroidManifest.xml'
           		java {
                    srcDirs = ['src', '../GameLibrary/src']
                    excludes = ['**/swing/**', '**/skunkworks/**']
                }
         		res.srcDirs = ['res']
           		assets.srcDirs = ['assets']
                aidl.srcDirs = ['src']
        	}
	    }
    }
}
