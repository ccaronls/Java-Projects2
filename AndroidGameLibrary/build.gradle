apply plugin: 'com.android.library'

repositories {
    mavenLocal()
}

dependencies {
    implementation files('../DnsAndroid/mdnsjava-2.1.6-SNAPSHOT.jar', '../DnsAndroid/dnsjava-2.1.7.jar')//, '../GameLibrary/build/libs/GameLibrary.jar')
//    compileOnly project(path: ':GameLibrary', configuration: 'androidLib')
//    implementation ('cc.lib:gamelib-android:1.0') {

//    }
    compileOnly project (path: ':GameLibrary', configuration: 'androidLib')
//    implementation files('../GameLibrary/build/libs/AndroidGameLibrary.jar')
//    compile project (':DnsAndroid')
//    apiElements project(path: ':GameLibrary', configuration: 'androidLib')
}

android {

    compileOptions {
        sourceCompatibility project.sourceCompatibility
        targetCompatibility project.targetCompatibility
    }

    compileSdkVersion project.myCompileSdkVersion.toInteger()
    buildToolsVersion project.myBuildToolsVersion

    defaultConfig {
	    minSdkVersion project.myMinSdkVersion.toInteger()
        targetSdkVersion project.myTargetSdkVersion
        versionCode 1
	    versionName '1.0.0'

    	sourceSets {
        	main {
           		manifest.srcFile 'AndroidManifest.xml'
           		java {
                    srcDirs = ['src']
                }
         		res.srcDirs = ['res']
           		assets.srcDirs = ['assets']
                aidl.srcDirs = ['src']
        	}
	    }
    }
}