<resources xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <style name="HomeButtonStyle" parent="@android:style/Widget.Button.Inset">
        <item name="android:layout_margin">5dp</item>
    </style>
    
    <style name="textViewScore" parent="@android:style/Widget.TextView">
        <item name="android:textColor">#f00</item>
        <item name="android:background">#4000</item>
        <item name="android:padding">5dp</item>
    </style>

    <style name="ttt_cell">
        <item name="insets">30dp</item>
        <item name="strokeWidth">20dp</item>
        <item name="color">#f00</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:layout_weight">1</item>
    </style>
    
    <style name="keypad" parent="@android:style/Widget.Button.Inset">
        <item name="android:textSize">38dp</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
