apply plugin: 'java'
apply plugin: 'com.google.devtools.ksp'
apply plugin: 'kotlin'

dependencies {
    implementation project(':GameLibrary')
    implementation(project(":KSPCommon"))
    ksp(project(":KSPBinarySerializer"))
}

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}

ksp {
    arg("package", "cc.game.superrobotron")
}