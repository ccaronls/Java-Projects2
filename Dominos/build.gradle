apply plugin: 'java'
apply plugin: 'application'

dependencies {
    compile project(':GameLibrary')
    compile project(':jmdns')
}

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }

        resources {
            srcDirs = ['images'];
        }
    }
}

//mainClassName = 'cc.game.dominos.swing'
mainClassName = 'cc.game.dominos.console.TestService'

repositories {
    mavenCentral()
}
