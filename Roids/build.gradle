apply plugin: 'java'
apply plugin: 'application'

sourceSets {
    main {
        java {
            srcDirs = ['src', '../GameLibrary/src']
            //exclude '**/swing/**'
            exclude '**/skunkworks/**'
        }
    }
}

mainClassName = 'cc.game.roids.applet.RoidsApplet'

repositories {
    mavenCentral()
}
