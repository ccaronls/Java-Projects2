apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'cc.app.test.console.TestService'

dependencies {
    implementation project(':GameLibrary')
}

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}