apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'cc.game.slot.SlotMachine'

dependencies {
    implementation project(':GameLibrary')
    implementation project(path: ':SwingApps', configuration: 'swingLib')
}

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}